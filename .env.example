# Copy this file to .env and adjust as needed (optional — sensible defaults are built in).

PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string-in-production
DB_PATH=./data/parole_dss.sqlite
NODE_ENV=development
